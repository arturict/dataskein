# DataSkein v0.1.5

DataSkein v0.1.5 completes the public workspace routing fix.

## Fixed

- Point Vercel's `/app` SPA rewrites at the extensionless root document required by `cleanUrls`.
- Keep the production routing contract covered by an automated configuration test.

The product scope, local-first data handling, and release artifact format are unchanged from v0.1.0.
